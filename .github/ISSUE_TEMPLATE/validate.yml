name: Validate Requirements
description: Run Agent 2 — static + semantic validation with confidence scoring
labels: ["validate"]
body:
  - type: textarea
    id: source_file
    attributes:
      label: Source code
      description: Paste the code to validate
      render: python
    validations:
      required: true
  - type: textarea
    id: requirements
    attributes:
      label: Requirements
      description: "List requirements, one per line: REQ-001: description"
    validations:
      required: true
